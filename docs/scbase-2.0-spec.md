# 超级细胞营地 2.0 改版 Spec

状态：v0.6，2.0 基础改造与卡牌百科第一版完成，AdSense 审核中
日期：2026-07-15
目标：把超级细胞营地从文章流型站点升级为超级细胞游戏中文资料与攻略中心，提升用户价值、搜索质量信号和 AdSense 审核通过率。

相关文档：[SCBase 运维手册](./scbase-operations.md)、[皇室战争卡牌与卡组系统 Spec](./scbase-clash-royale-cards-decks-spec.md)

## 0. 已确认决策

- 站点中文名：统一使用“超级细胞营地”，`SCBase` 主要保留为域名和内部技术命名。
- 首页定位：超级细胞游戏中文玩家营地，第一阶段内容权重明显偏向皇室战争。
- URL 结构：已新增 `/clashroyale/`、`/brawlstars/`、`/clashofclans/`、`/moco/`、`/tools/` 顶级入口；旧文章 URL 暂不迁移。
- 构建产物：`public/` 已从 Git 跟踪中移除并加入 `.gitignore`；原先需要保留的 `ads.txt`、`about.webp`、`kl/` 工具资源已迁移到 `static/`。
- Phase 1/2 基础改造：信任页、首页、栏目页、文章页基础模板、工具页、结构化数据和新文章 archetype 已初步落地。
- 审核状态：AdSense 已提交审核。审核期间优先保持站点稳定，暂停旧文章批量重写、批量删除、批量 noindex 和 URL 迁移。
- 视觉方向：首页、频道页、工具页和文章页已完成第一轮统一；全站采用适中的中文字号和字重，正文优先保证阅读舒适度，标题通过层级而非普遍加粗建立重点。
- 内容标准：之后凡是由 Codex 参与整理、创作或改写的超级细胞营地文章，默认同时按公众号阅读体验和 AdSense/搜索质量标准把控。

## 1. 背景

当前站点主要以 Hugo 文章发布为核心，内容覆盖皇室战争、荒野乱斗、部落冲突、Mo.Co 等 Supercell 游戏。现有结构偏博客流，首页和栏目页对内容价值的组织较弱，Google 可能更容易把它识别为低原创度、薄内容、重复归档页面较多的站点。

超级细胞营地 2.0 的目标不是单纯换 UI，而是重构内容表达方式：

- 从“发布文章”转向“沉淀可长期引用的攻略与资料”。
- 从“最新内容列表”转向“游戏入口 + 专题中心 + 工具/资料入口”。
- 从“搜索引擎抓到大量薄页面”转向“抓到少量但结构清晰的高价值页面”。

## 2. 产品定位

超级细胞营地是面向中文玩家的超级细胞游戏攻略、资料与工具站。

第一阶段以皇室战争为主站点核心，其他游戏保留入口但降低薄页面暴露：

- 皇室战争：主力内容，优先建设栏目、专题、工具和长期攻略。
- 荒野乱斗：保留新闻和攻略入口，后续补齐资料页。
- 部落冲突：保留栏目入口，聚合更新与活动。
- Mo.Co：保留观察和资讯入口。

## 3. 成功指标

短期目标：

- 站点首页能在 10 秒内让新用户理解“这里能帮我解决什么问题”。
- 皇室战争栏目页不再只是文章列表，而是可导航的攻略中心。
- 新文章具备统一的信息增量结构，避免纯资讯感。
- 补齐 AdSense 常见信任页：关于、联系方式、隐私政策、版权声明、免责声明。

中期目标：

- Google Search Console 中低质量/已抓取未编入页面比例下降。
- 核心页面获得更多自然搜索入口。
- 用户从首页进入文章或专题的路径更清晰。

## 4. 信息架构

### 4.1 顶部导航

建议导航：

- 首页
- 皇室战争
- 荒野乱斗
- 部落冲突
- Mo.Co
- 工具
- 关于

说明：

- 不建议把标签、归档、全部分类直接暴露为主要入口。
- “工具”可以先是占位栏目，后续接卡组推荐、活动日历、版本追踪等。

### 4.2 首页

首页定位为统一资讯入口，游戏百科、攻略和工具的详细功能继续放在各游戏二级页面。

当前结构：

1. 置顶内容
   - 左侧一篇主内容，右侧四篇次要内容。
   - 移动端主内容独占一行，四篇次要内容采用 2×2 宫格。
2. 热门内容
   - 横向轮播，支持分页、循环、键盘焦点暂停和减少动画设置。
3. 最新资讯
   - 桌面端 3 列共 12 篇，移动端为左图右标题和日期的列表。
4. 常用工具
   - 站内查询工具与可靠的外部功能入口。
5. 游戏频道
   - 皇室战争、荒野乱斗、部落冲突和 Mo.Co 二级入口。

首页编排使用文章 Front Matter：

```yaml
home_pinned: 1
# home_popular: 1
```

- `home_pinned` 使用 1-5 控制置顶顺序。
- `home_popular` 使用正整数控制热门顺序。
- 同一文章不应同时配置两个字段。
- 配置不足时模板使用未重复的最新文章补位；最新资讯排除置顶和热门后取 12 篇。

### 4.3 皇室战争栏目页

皇室战争栏目页应成为第一阶段最重要的 SEO 和用户入口。

推荐结构：

1. 栏目头部
   - 标题：皇室战争攻略中心
   - 描述：卡组推荐、版本更新、活动挑战、新卡解析与实用指南。

2. 快捷入口
   - 最新卡组
   - 新手指南
   - 版本更新
   - 活动挑战
   - 卡牌解析

3. 当前赛季重点
   - 最新赛季文章
   - 当前强势卡组
   - 当前版本变化

4. 长期指南
   - 如何组卡
   - 卡组原型介绍
   - 超级选卡攻略
   - 新手升级建议

5. 最新文章列表
   - 放在页面后半部分，作为补充。

### 4.4 文章页

文章页保留现有 Hugo Markdown 工作流，但增加标准化内容模块。

建议每篇文章包含：

- 标题
- 更新时间
- 摘要
- 适合人群
- 正文
- 关键结论
- 相关攻略
- FAQ
- 资料来源或说明

Front matter 可逐步扩展：

```yaml
title:
description:
date:
updated:
image:
game: clashroyale
content_type: deck-guide
difficulty: beginner | intermediate | advanced
evergreen: true
featured: false
home_pinned: 1 # 与 home_popular 二选一
faq:
  - question:
    answer:
related:
  - /posts/...
```

第一阶段不要求所有旧文章补齐；AdSense 审核期间也不做旧文章批量重写。新文章应默认按此结构写。

## 5. 内容类型

### 5.1 长期攻略

长期攻略是超级细胞营地 2.0 的核心质量资产。

示例：

- 皇室战争如何构建一套合理卡组
- 皇室战争卡组原型介绍
- 超级选卡挑战攻略
- 新手升级资源分配指南

要求：

- 能在 3-12 个月后仍然有价值。
- 标题避免纯新闻口吻。
- 提供清晰步骤、判断标准和常见误区。

### 5.2 卡组推荐

卡组推荐要从“列卡组”升级为“解释为什么可用”。

建议结构：

- 适合玩家
- 卡组代码或卡牌列表
- 核心打法
- 起手建议
- 对局思路
- 可替换卡牌
- 优缺点

### 5.3 版本更新

版本更新不只是翻译公告。

建议结构：

- 更新重点
- 对普通玩家的影响
- 强势/弱势变化
- 建议优先做什么
- 相关攻略入口

### 5.4 资讯短文

资讯可以保留，但需要降低站点主体占比。

处理策略：

- 短资讯可以归入周报或专题汇总。
- 避免大量 300-500 字的独立薄页面。
- 无长期价值的旧资讯可考虑 noindex。

## 6. SEO 与 AdSense 改进

### 6.1 必备信任页

已初步完成：

- 关于我们
- 联系方式
- 隐私政策
- 版权声明
- 免责声明

### 6.2 薄页面处理

重点检查：

- 空分类页
- 低内容标签页
- 分页页
- 重复归档页
- 未命名页面

后续处理方案：

- 对低价值标签页加 `noindex,follow`。
- 只保留有真实专题价值的标签入口。
- 分类页增加栏目介绍和精选内容。
- 分页页减少在首页和导航中的权重。

当前状态：

- AdSense 审核期间暂不批量处理薄页面，避免抓取状态频繁波动。
- 只修明显错误，例如坏图、构建错误、默认标签、缺失 description。

### 6.3 结构化数据

优先添加：

- WebSite
- Organization
- Article
- BreadcrumbList
- FAQPage

### 6.4 内部链接

每篇新文章至少包含：

- 1-3 个相关站内文章链接。
- 1 个栏目页或专题页链接。
- 必要时链接到工具页。

## 7. 视觉与交互方向

整体方向：

- 少营销感，多资料站/攻略站感。
- 首页和栏目页要更像“可探索的目录”，不是单纯博客。
- 卡片信息密度适中，避免只有大图和标题。
- 移动端优先保证阅读体验。

视觉关键词：

- 清晰
- 可信
- 游戏感适度
- 资料密度高
- 入口明确

## 8. Hugo 实现建议

### 8.1 新增模板

建议新增或改造：

- `layouts/index.html` 或主题首页覆盖模板
- `layouts/section/posts.html`
- `layouts/section/clashroyale.html`
- `layouts/partials/content/game-card.html`
- `layouts/partials/content/guide-card.html`
- `layouts/partials/seo/schema.html`

### 8.2 内容数据

可新增：

- `data/games.yaml`
- `data/home.yaml`
- `data/featured.yaml`

示例：

```yaml
clashroyale:
  name: 皇室战争
  description: 卡组推荐、版本更新、挑战攻略与机制解析
  url: /posts/clashroyale/
  color: blue
```

### 8.3 Front matter 兼容

现有字段继续保留：

- `title`
- `image`
- `description`
- `date`
- `slug`
- `categories`
- `tags`
- `keywords`

新增字段应可选，避免一次性迁移所有旧文章。

## 9. 分阶段计划

### Phase 1：基础质量修复

目标：降低 AdSense 低质量风险。

状态：基本完成，审核期间只做低风险修补。

已完成：

- 补齐关于、联系方式、隐私政策、版权声明、免责声明。
- 修复首页封面图、卡片信息、栏目入口。
- 为新文章建立写作模板。
- 清理 `public/` 构建产物入库问题。
- 统一站点中文名为“超级细胞营地”。

暂缓：

- 清理或 noindex 低价值标签/分页/归档页。
- 旧文章批量重写和批量 front matter 迁移。

### Phase 2：首页与皇室战争栏目页

目标：让用户和搜索引擎看懂站点核心价值。

状态：新首页已发布并完成线上验收，进入审核期稳定运行阶段。

已完成：

- 重做首页信息架构。
- 重做皇室战争栏目页。
- 增加精选攻略和长期指南入口。
- 加强站内链接。
- 新增荒野乱斗、部落冲突、Mo.Co 通用频道页。
- 移动端入口卡片已改为左 icon、右描述的紧凑布局。
- 首页已调整为置顶、热门、最新资讯、常用工具和游戏频道结构。
- 首页支持 `home_pinned`、`home_popular` 编排、自动补位和跨模块去重。
- 首页图片已接入 Hugo WebP 处理，最新资讯桌面和移动端共用一套 DOM。

已完成的视觉优化：

- 统一首页、频道页和工具页的 icon 容器与基础风格。
- 建立全站基础色、各游戏轻量区分色，以及统一的按钮、标签和卡片规则。
- 压缩首页、频道页和工具页的移动端信息高度。
- 完成文章详情页标题区、正文、表格、视频、标签和相关阅读的响应式整理。
- 清理重点页面中无实际信息价值的英文眉题和装饰标签。
- 降低导航、栏目标题和资讯卡片的字号与字重，减轻中文页面整体视觉压力。

### Phase 3：内容资产建设

目标：形成可长期排名的核心内容。

状态：进行中。

任务：

- 写/改 5-10 篇长期攻略。
- 建立卡组推荐标准格式。
- 建立版本更新标准格式。
- 为重点文章增加 FAQ。
- 新文章统一兼顾公众号阅读体验和 AdSense/搜索质量。
- 视频引用文章不能只嵌播放器，需要补充原创摘要、分析、FAQ 和资料来源。

审核期间策略：

- 可以正常发布高质量新文章。
- 暂不批量处理旧文章，等 AdSense 审核结果后再决定力度。

### Phase 4：工具与数据化页面

目标：形成区别于普通博客的独特价值。

状态：静态搜索、免费奖励和皇室战争卡牌百科第一版已完成；卡组榜单与查询类工具待合法数据源和接口服务。

已完成：

- 使用 Hugo 自动生成静态搜索索引。
- 使用 Fuse.js 实现文章和工具的中文模糊搜索。
- 搜索页支持文章/工具筛选、URL 查询参数和移动端布局。
- 搜索入口改为 header 放大镜按钮，不占用主导航文字项。
- 搜索页设为 `noindex`，且不进入 sitemap。
- 将现有工具入口提取到 `data/tools.yaml`，供工具页和搜索索引共享。
- 上线免费奖励总入口及皇室战争、荒野乱斗、部落冲突三个游戏视图，奖励数据和本地图片统一由仓库维护。
- 完成 126 张皇室战争基础卡牌、55 个已上线觉醒/精英形态的资料、详情页、中文实战内容和版本历史。
- 将卡牌页面入口迁移到 `content/clashroyale/cards/`，结构化数据保存在 `data/clashroyale/`，模板与内容数据职责分离。

候选工具：

- 按模式和统计周期生成的皇室战争 Top 20-30 卡组榜单
- 玩家、部落和常用游戏数据查询
- 活动/挑战日历
- 版本更新追踪页

## 10. 第一批建议落地任务

当前状态：

- 已完成：新增基础信任页面。
- 已完成：重做首页为游戏攻略入口页。
- 已完成：重做皇室战争栏目页。
- 已完成：新增文章 archetype，规范新文章结构。
- 已完成：加 Article、Breadcrumb、FAQ 等结构化数据基础能力。
- 暂缓：为低价值标签页和分页页加 noindex 策略。
- 暂缓：把 3-5 篇已有皇室战争高价值文章改成长期攻略格式。

审核期间优先级：

1. 保持线上站点稳定。
2. 修构建错误、坏图、明显 front matter 错误。
3. 按统一质量标准发布新文章。
4. 观察 AdSense 审核和 Search Console 状态。

审核结果出来后的优先级：

1. 如果通过：继续视觉打磨和内容资产建设，旧文温和优化。
2. 如果未通过：根据拒审原因集中处理旧内容、薄页面和低价值归档。

## 11. 下一阶段规划

当前基础结构、首页、频道页、搜索、奖励和卡牌百科第一版已经跑通。下一阶段以线上稳定为前提，优先处理卡组榜单的数据来源、卡牌搜索接入和长期内容资产，不再进行大范围视觉改版。

优先级建议：

1. P0：审核期间线上稳定与低风险修补。
2. P1：卡牌百科维护、搜索接入与新增卡牌更新流程。
3. P2：卡组榜单采集 MVP 和数据质量验证。
4. P3：新文章与长期内容资产持续建设。
5. P4：玩家查询、部落查询等接口工具技术预研。

### 11.0 P0：新首页发布闭环

目标：确保新首页从本地实现安全进入线上，不把代码整理或后续功能混入发布阻断项。

已完成：

- [x] 正式首页替换旧版首页，删除 `/home-prototype/`。
- [x] 实现置顶 5 篇、热门 5 篇、最新资讯 12 篇的编排、去重和补位。
- [x] 合并最新资讯桌面/移动端 DOM，修复重复图片加载。
- [x] 修复 320px 窄屏置顶小卡文字裁剪。
- [x] 补齐首页唯一 H1、title、description 和 canonical。
- [x] 通过 320、390、768、1280px 本地响应式检查。
- [x] 通过 Hugo `--panicOnWarning` 生产构建和关键本地链接检查。

待完成：

- [x] `home_pinned`、`home_popular` 由运维人工维护顺序和互斥关系，不增加构建校验。
- [x] 完成正式首页本地视觉确认。
- [x] 审查 Git 变更范围，提交并推送当前改造。
- [x] 确认 Vercel Git 部署成功；仓库当前未配置 GitHub Actions 工作流。
- [x] 在线检查首页 H1、canonical、sitemap、图片、轮播和关键入口。
- [ ] 发布后保持结构稳定，继续观察 AdSense 与 Search Console。

P0 验收标准：

- Hugo 生产构建无错误、无配置警告。
- 首页置顶、热门、最新资讯之间没有重复文章。
- 根首页和关键入口返回 200，`/home-prototype/` 返回 404。
- 线上 HTML 只有一个 H1，canonical 指向 `https://scbase.cn/`。
- Vercel 部署成功且移动端不存在明显溢出、重叠或空白模块。

### 11.0.1 P1：首页模板组件化

状态：已完成首页模板组件化和首页图片渲染逻辑收敛。

- [x] 将首页选择、排序、去重和补位逻辑提取为 `partials/home/portal-data.html`。
- [x] 将置顶、热门、最新资讯、常用工具和游戏频道分别提取为 home partial。
- [x] 保持 `layouts/index.html` 只负责页面组合和资源加载。
- [x] 将本地 WebP 裁切、远程图片回退、`srcset`、懒加载和首图优先级收敛到统一的首页图片 partial。
- [x] 使用生产构建逐字节对比，确认重构前后首页 HTML、文章顺序和图片加载属性一致。
- [x] 通过 320、390、1280px 浏览器回归检查，确认响应式行为和轮播初始化正常。

### 11.1 Icon 与视觉资产

目标：减少临时感，让站点更像稳定运营的玩家营地。

状态：第一轮已完成。

任务：

- [x] 统一首页、栏目页、工具页里的 icon 容器、尺寸和对齐规则。
- [x] 为皇室战争、荒野乱斗、部落冲突、Mo.Co、工具等入口建立稳定的区分色。
- [x] 检查移动端 icon 尺寸、对齐和留白，使用左 icon、右文案的紧凑卡片。
- [ ] 站内自有图标暂不制作，待工具和百科模块扩展后再统一评估。

### 11.2 布局与信息密度

目标：让页面更容易扫读，尤其是移动端。

状态：第一轮已完成。

任务：

- [x] 压缩首页和栏目页移动端首屏高度，减少重复下滑。
- [x] 细化首页主要模块、入口卡片、最新文章和 footer 的间距节奏。
- [x] 统一首页、皇室战争页和通用游戏频道页的组件语言。
- [x] 检查 footer、工具页、列表页和文章页在窄屏下的换行与视觉重心。
- [x] 保持内容站所需的信息密度，不采用营销落地页式布局。

### 11.3 UI 配色与整体风格

目标：形成“玩家营地”气质，而不是默认模板或纯资料表格感。

状态：第一轮已完成，后续只做小范围视觉修正。

方向：

- 主色避免单一蓝色统治全站，可尝试用黑白基础、少量高饱和游戏色作为点缀。
- 皇室战争、荒野乱斗、部落冲突、Mo.Co 可以有轻量区分色，但全站仍保持统一。
- 按钮、标签、卡片边框、hover 状态需要统一一套规则。
- 谨慎使用渐变和大面积装饰，优先保证可读性、速度和内容可信度。
- 中文标题不普遍使用 `800-900` 的极重字重；导航以 `600`、普通卡片标题以 `700`、主视觉标题以 `750` 左右为基准。
- 正文和摘要保持常规字重，避免为了增强层级同步放大所有文字。

### 11.4 搜索能力

目标：当文章和工具数量增加后，让用户能直接找到卡组、更新、工具或关键词文章。

状态：第一版已完成并通过线上校验。

候选方案：

- 第一阶段：静态站内搜索，基于 Hugo 生成 JSON 索引，前端本地搜索。
- 第二阶段：接入 Pagefind、FlexSearch 或 Fuse.js，支持标题、摘要、标签、游戏分类搜索。
- 第三阶段：如果内容数据化，再考虑服务端搜索或数据库查询。

第一版实施决定：

- [x] 使用 Hugo 生成静态 JSON 索引，不增加服务端和数据库。
- [x] 浏览器端使用 Fuse.js 做模糊匹配，保持现有 Hugo/Vercel 构建方式。
- [x] 首批索引覆盖文章和现有工具入口，不索引标签归档、分页和其他低价值列表页。
- [x] 搜索结果展示标题、所属游戏或工具类型、更新时间和摘要。
- [x] 搜索入口使用 header 放大镜按钮，不在首页增加大型搜索模块。
- [x] 搜索页设置 `noindex` 并从 sitemap 排除。

注意事项：

- 搜索入口不应过早喧宾夺主，先放在 header 或工具页中轻量试用。
- 搜索结果需要展示所属游戏、内容类型、更新时间和摘要。
- 避免把低质量标签页重新暴露成搜索噪音。

### 11.5 接口与数据库方向

目标：为后续工具和资料型页面预留技术路线。

可能场景：

- 皇室战争卡组、卡牌、活动、版本变更等结构化资料。
- 工具页读取接口数据，例如玩家查询、卡组推荐、活动日历。
- 内容和工具之间共享数据，而不是散落在 Markdown 正文里。

阶段建议：

- 先保持 Hugo 静态站为主，数据以 JSON/YAML/TOML 文件进入仓库，方便版本管理和部署。
- 当需要频繁更新、用户查询或第三方 API 聚合时，再引入轻量接口。
- 如果后续接数据库，优先考虑只让工具页面读接口，文章页仍保持静态生成，降低部署复杂度。

开放问题：

- 数据源优先放在仓库文件、外部 API，还是独立数据库？
- 是否需要为工具页单独拆出前端应用，还是继续嵌在 Hugo 静态页面中？

已确定：搜索当前覆盖文章和工具；下一步将已完成的基础卡牌详情加入同一索引，觉醒和精英子页暂不单独加入，避免重复结果。

## 12. 待讨论问题

- 资讯类短文是否继续发布，还是合并成周报？
- AdSense 审核通过或未通过后，旧文章应该温和优化还是集中重写？
- 工具数据优先放在仓库文件、外部 API，还是独立数据库？

## 13. 当前执行优先级

### P0：线上稳定与审核观察

- 保持当前首页、频道页、文章页、工具页和搜索结构稳定。
- 按统一质量标准发布新文章，继续观察 AdSense 与 Search Console。
- 只修复构建错误、坏图、失效链接和明显 front matter 问题。
- AdSense 结果出来前不批量重写旧文章，不集中调整归档和标签策略。

### P1：皇室战争卡牌百科维护与搜索接入

状态：卡牌百科第一版完成；进入新增卡牌维护、数据复核和搜索接入阶段。

- [x] 完成 126 张基础卡牌及 55 个已上线觉醒/精英形态的数据与页面。
- [x] 补齐基础卡牌的中文特点、使用要点、对局分析和人工复核后的完整版本历史。
- [x] 固化 Fandom 导入、人工编辑、历史覆盖、图片本地化、完整审计和响应式验收流程。
- [x] 将卡牌路由入口集中到 `content/clashroyale/cards/`，结构化数据继续由 `data/clashroyale/` 管理。
- [ ] 将 126 张基础卡牌加入 Fuse.js 搜索索引；觉醒和精英子页暂不单独进入搜索结果。
- [ ] 后续新卡、觉醒、精英或平衡性调整上线时，按运维手册完成增量更新与全量校验。

详细方案见 [`scbase-clash-royale-cards-decks-spec.md`](scbase-clash-royale-cards-decks-spec.md)。

### P2：卡组榜单采集与展示

状态：原静态卡组库原型不进入正式产品；等待合法数据源后改为模式榜单。

- [x] 使用静态数据验证 8 卡渲染、归一化、详情和移动端布局。
- [ ] 使用固定 IP 服务采集官方 API 战斗日志并生成榜单快照。
- [ ] 首批支持天梯、皇室征程、终极挑战、经典挑战和特殊活动。
- [ ] 每个模式展示 Top 20-30，支持 1 天、3 天、7 天和综合/使用率/胜率排序。
- [x] 增加卡组数据完整性、图片、费用和四卡循环校验。
- [ ] 榜单具备真实统计数据后再加入皇室战争频道和 Fuse.js 搜索索引。

### P3：长期内容资产

- 按统一标准持续发布高质量资讯、解读和长期攻略。
- 优先完成 3-5 篇皇室战争常青内容，而不是批量加工全部旧文。
- 为重点内容补充 FAQ、资料来源、相关工具和站内链接。
- 视频类文章继续要求原创摘要、背景、观点和结论，避免只有播放器。

### P4：接口工具技术预研

- 盘点皇室战争、荒野乱斗和部落冲突官方接口能力与限制。
- 评估使用闲置服务器提供固定出口、密钥保护和轻量缓存。
- 明确 Hugo 静态页面、Vercel 和独立接口服务之间的职责。
- 此阶段只出技术方案，不立即引入数据库或重构站点。

### 条件任务：AdSense 结果后处理

- 如果审核通过：温和优化高价值旧文，逐步补齐 front matter 和内链。
- 如果审核未通过：根据拒审原因集中处理薄内容、低价值归档、标签页和旧文章质量。
